export type VoiceNote = {
  id: string;
  title: string;
  transcript: string;
  audioUrl: string;
  originalFilename: string;
  mimeType: string;
  fileSize: number;
  wordCount: number;
  createdAt: string;
  updatedAt: string;
};
